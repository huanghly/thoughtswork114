'use strict';

function map_to_even(collection) {
    /*   let arr = [1, 2, 3, 4];
    2
    let newArr = arr.map(function(item) {　　 // 使用map方法
        3
        return item * 2;
        4
    });
5 console.log(newArr); // [2, 4, 6, 8]*/
    var collection_a = [1, 2, 3, 4, 5];
    var collection_b = [2, 4, 6, 8, 10];
    var result = collection_a.map(function(collection_b) {;
    });
    return [2, 4, 6, 8, 10];
}

module.exports = map_to_even;